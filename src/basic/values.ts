import helperCreateGetObjects from '../helpers/helperCreateGetObjects'

/**
  * 获取对象所有值
  *
  * @param {Object} obj 对象/数组
  * @return {Array}
  */
const values = helperCreateGetObjects('values', 0)

export default values
