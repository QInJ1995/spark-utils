import helperCreateGetObjects from '../helpers/helperCreateGetObjects';

// 使用 helperCreateGetObjects 创建 entries 函数，用于获取对象的可枚举属性键值对数组
const entries = helperCreateGetObjects('entries', 2);

export default entries;
