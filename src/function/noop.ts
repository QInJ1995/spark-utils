/**
 * 一个空的方法（移植自旧 src/function/noop.js）
 * 始终返回 undefined，可用于初始化值；任意参数均被忽略。
 */

/**
 * 空函数
 *
 * @param _args 任意参数（忽略）
 * @returns undefined
 */
export function noop(..._args: unknown[]): void {
  // 旧版为空函数体，隐式返回 undefined
}
